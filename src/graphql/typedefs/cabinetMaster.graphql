input add_cabinet_master {
    cabinet: String
    is_active: Boolean
    created_by: String
    created_date: String
}

input edit_cabinet_master {
    id: Int!
    cabinet: String!
    is_active: Boolean
    created_by: String
    created_date: String
}

type cabinet_master {
    id: Int
    cabinet: String
    is_active: Boolean
    created_by: String
    created_date: String
}