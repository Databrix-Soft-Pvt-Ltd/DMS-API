type subFolderMapping {
    id: Int
    userId: Int
    subFolderId: Int
    createdBy: Int
    createdDate: String
}

input addSubFolderMapping {
    userId: Int!
    subFolderIds: String!
    createdBy: Int
}

input deleteSubFolderMapping {
    id: Int
    userId: Int
    subFolderId: Int
}