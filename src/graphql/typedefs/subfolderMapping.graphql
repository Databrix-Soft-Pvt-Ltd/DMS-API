type subFolderMapping {
    id: Int
    user_id: Int
    sub_folder_id: Int
}

input addSubFolderMapping {
    user_id: Int!
    sub_folder_id: Int!
}

input deleteSubFolderMapping {
    id: Int
    user_id: Int
    sub_folder_id: Int
}